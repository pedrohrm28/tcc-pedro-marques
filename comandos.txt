============================================================
 COMANDOS — Fase 5 (base nova) + agregacao final
============================================================
Rode num PowerShell na raiz do projeto:
  cd "C:\Users\paula\source\repos\tcc-pedro-marques"

Pre-requisito: Ollama no ar com os 3 modelos (llama3.1:8b, qwen2.5:3b, llama3.2:3b).
  Verificar:  & "$env:LOCALAPPDATA\Programs\Ollama\ollama.exe" list

DICA DE RAM (maquina de 7,8GB): feche o VS Code e apps pesados antes de rodar os LLMs,
senao o modelo recarrega do disco a cada sentenca (lento). Numa maquina mais potente,
nao precisa. Tudo e RETOMAVEL: se travar/fechar, re-rode o MESMO comando que continua.

------------------------------------------------------------
 ESTADO ATUAL (ja prontos — NAO precisa rodar)
------------------------------------------------------------
 base_mapeada: TODOS os 5 modelos x 2 tarefas        -> COMPLETO
 base_nova:    crf/ner (100) e regras/upos (100)     -> COMPLETO
 base_nova:    os 3 LLMs                              -> FALTAM (passo 1 abaixo)

------------------------------------------------------------
 1) LLMs na BASE NOVA  (NER CoNLL-2003 + UPOS GSD, 100 sent cada)
    Cada comando roda NER e UPOS do modelo. Rapidos primeiro, 8B por ultimo.
------------------------------------------------------------
python rodar_base_nova.py --modelo qwen2.5:3b
python rodar_base_nova.py --modelo llama3.2:3b
python rodar_base_nova.py --modelo llama3.1:8b

# (Alternativa: rodar tudo de uma vez; pula crf/regras que ja estao feitos)
# python rodar_base_nova.py --todos

------------------------------------------------------------
 2) Conferir se o modelo esta residente (opcional, 2o terminal)
------------------------------------------------------------
& "$env:LOCALAPPDATA\Programs\Ollama\ollama.exe" ps    # UNTIL Forever = bom (rapido)

------------------------------------------------------------
 3) Agregar a BASE NOVA (so depois dos 3 LLMs terminarem)
    Aplica o mapeamento de tipos NER {PER,ORG,LOC,MISC} no gold E nas predicoes.
------------------------------------------------------------
python agregar.py --base base_nova

# Saidas em resultados/base_nova/:
#   tabela_ner.md / .csv, tabela_upos.md / .csv, discrepancias.csv, *.metricas.json

------------------------------------------------------------
 4) (Ja feito) Re-agregar a base mapeada, se quiser
------------------------------------------------------------
python agregar.py --base base_mapeada

============================================================
 Na MAQUINA NOVA (do zero): instalar e clonar
============================================================
winget install Git.Git
winget install Python.Python.3.11
winget install Ollama.Ollama
# feche e reabra o terminal apos instalar

git clone https://github.com/pedrohrm28/tcc-pedro-marques.git
cd tcc-pedro-marques
pip install -r requirements.txt
ollama pull llama3.1:8b ; ollama pull qwen2.5:3b ; ollama pull llama3.2:3b

# Datasets da base nova (eng.testb, pt_gsd-ud-test.conllu) e ner.csv NAO estao no git.
# Copie a pasta datasets/ manualmente (pendrive/Drive) se for rodar na maquina nova.
